{
  "name": "cc-channels",
  "version": "0.1.0",
  "private": true,
  "description": "Channel plugins for Claude Code — connect messaging platforms via MCP",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "clean": "pnpm -r exec rm -rf dist"
  },
  "keywords": ["claude-code", "mcp", "channels", "telegram", "feishu", "lark"],
  "author": "Tom Zhou",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
